# {{AGENT_NAME}} — Long-Term Memory

Distilled, durable facts about this domain: patterns, key context, standing preferences,
recurring decisions. Read at the start of every session. Keep it short — this is the
distillate, not the archive (the archive is `wiki/` and `log.md`).

*(empty — fill as patterns emerge)*
